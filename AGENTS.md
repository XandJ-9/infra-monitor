# AGENTS.md

本文件用于指导 Codex 等 AI coding agent 在 Infra Monitor 项目中进行需求分析、代码修改、测试验证和文档维护。进入本仓库后，先阅读本文件，再按任务需要阅读 `README.md`、`doc/architecture.md`、`doc/development.md` 和相关源码。

## 项目总结

Infra Monitor 是一个轻量级基础设施监控面板，面向可信内网或本地运维环境，用于快速查看 ZooKeeper、Kafka、Elasticsearch 和服务器文件的状态与基础信息。

项目采用 FastAPI + Jinja2 + Bootstrap 5 的传统 Web 应用结构，后端按 `routers` 和 `services` 分层：

- `app/routers/` 负责 HTTP 参数、页面渲染、JSON API 和异常映射。
- `app/services/` 负责组件连接、外部 API 调用、文件系统访问和业务数据整理。
- `app/templates/` 与 `static/` 负责页面结构、样式和少量前端交互。
- `app/config.py` 负责默认配置、配置加载、旧配置迁移、归一化和保存。
- `tests/` 使用 pytest 覆盖服务层、配置层和路由层行为。

当前项目的关键质量目标是：

- 单个外部组件异常时，页面仍能访问，并返回可展示的错误状态。
- 所有外部访问都应设置超时，避免监控页被下游组件拖死。
- 文件浏览器必须保持只读，并且严格限制在配置根目录内。
- 默认测试不依赖 ZooKeeper、Kafka、Elasticsearch 等外部组件。
- 新功能优先沿用现有分层、配置和测试方式，不引入不必要的新框架。

## 工作流程

### 1. 先读上下文，再改代码

接到需求后，优先阅读：

- `README.md`：确认项目能力、启动方式和文档入口。
- `doc/architecture.md`：确认模块边界、页面/API 和关键约束。
- `doc/development.md`：确认本地启动、测试和提交前检查。
- 与需求直接相关的 `app/routers/`、`app/services/`、`tests/` 文件。

不要只根据文件名猜测实现。尤其涉及配置、连接管理、路径安全、代理路径前缀和外部组件访问时，必须先读现有代码。

### 2. 先定修改边界

修改前明确：

- 改动属于页面展示、API、服务逻辑、配置结构、测试，还是文档。
- 是否影响现有接口响应结构。
- 是否影响 `config.json` 兼容性。
- 是否需要同步更新 `README.md` 或 `doc/`。
- 是否需要补充服务层测试、路由测试或集成测试说明。

若需求可以通过服务层完成，不要把业务逻辑塞进路由或模板。若只是页面展示问题，不要顺手重构后端。

### 3. 小步实现，优先复用现有模式

优先使用项目已有写法：

- FastAPI 路由继续放在对应 `app/routers/*.py`。
- 外部组件逻辑继续放在对应 `app/services/*.py`。
- 模板使用 `request.app.state.templates.TemplateResponse`。
- 配置读取通过 `load_config()`，配置保存通过 `save_config()`。
- 文件浏览相关异常应继承或复用 `FileBrowserError` 体系。
- 可展示状态优先使用 `ComponentStatus` 等现有模型。

新增抽象必须有明确收益：减少重复、隔离外部依赖、统一错误处理或稳定接口。不为单个小功能创建过度通用的工具层。

## 编码原则

### 路由层

- 路由层只处理参数读取、响应渲染和异常映射。
- API 返回结构应保持稳定，新增字段优先兼容旧前端。
- 用户输入要在进入服务层前做基本类型约束，复杂校验放服务层。
- 页面路由出错时应渲染可读错误，不应让整页 500。

### 服务层

- 服务层负责真实业务行为，包括外部连接、数据整理、边界校验和错误降级。
- ZooKeeper、Kafka、Elasticsearch 访问必须设置超时。
- 外部组件失败时，返回空列表、错误状态或带 `error` 的结构，避免异常穿透到页面。
- 不要在服务层引入与模板或 HTTP 响应强绑定的逻辑。

### 配置

- `config.json` 是运行时配置文件，代码必须兼容旧配置。
- 修改配置结构时，需要更新 `DEFAULT_CONFIG`、归一化逻辑和相关测试。
- 保留旧字段兼容性时，要在代码注释中说明原因。
- 不要把敏感配置打印到日志或返回到页面；密码类字段应只返回是否存在。

### 文件浏览器

文件浏览器是高风险模块，修改时必须遵守：

- 只读，不增加编辑、上传、删除、重命名、移动能力。
- 不允许绝对路径、`../` 路径穿越或指向根目录外的符号链接。
- 所有用户传入路径必须通过 `FileBrowserService.resolve_path()` 或同等安全逻辑。
- 文本预览必须受 `max_preview_bytes` 限制。
- 二进制文件不做文本预览。
- 图片预览只允许明确支持的 MIME 类型，并以内联预览方式返回。

### 前端

- 维持 Bootstrap 5 + 少量原生 JavaScript 的轻量方案。
- 不为简单交互引入大型前端框架。
- 页面应适配 Nginx `root_path` 代理前缀。
- 新增 fetch 请求时，注意使用模板注入或现有方式处理 `root_path`。
- 监控页面优先清晰、稳定、可扫描，不做重视觉装饰。

## 测试原则

默认验证命令：

```bash
uv run ruff check .
uv run pytest -q
docker compose config --quiet
```

默认测试不应依赖外部组件。需要真实连接 ZooKeeper、Kafka、Elasticsearch 时，必须显式启用集成测试：

```bash
docker compose up -d
RUN_CONNECTION_TESTS=1 uv run pytest -q -m integration
```

测试补充建议：

- 配置结构变化：补充 `tests/test_config.py`。
- 服务层逻辑变化：补充对应 `tests/test_*_service.py`。
- API 行为变化：补充对应路由测试。
- 文件浏览安全变化：补充路径穿越、符号链接、绝对路径、截断和二进制文件测试。
- 前端页面懒加载或模板链接变化：补充现有页面相关测试，必要时使用 Playwright 截图验证。

文档类改动可以不运行完整测试，但最终说明中应明确“未运行测试，因为仅修改文档”。

## 文档原则

- 根目录 `README.md` 保留项目介绍、能力概览、启动入口和文档索引。
- `AGENTS.md` 保留 agent 工作约定和项目级 coding 规则。
- 架构、接口、开发流程、测试策略和功能设计放入 `doc/`。
- 新增文档必须登记到 `doc/README.md`。
- 新增或变更对用户可见的能力时，同步更新相关设计文档或 README。
- 文档应写具体约束和操作方式，避免空泛口号。

## 自检清单

交付前逐项检查：

- 是否读过相关文档和现有实现。
- 是否保持 `routers` / `services` / `templates` 的职责边界。
- 是否没有破坏文件浏览器只读和路径边界。
- 是否所有外部调用都有超时和降级路径。
- 是否没有引入默认测试对外部组件的依赖。
- 是否补充或更新了必要测试。
- 是否更新了相关文档索引。
- 是否说明了已运行和未运行的验证命令。

## 不推荐做法

- 未读代码就大规模重构。
- 为一个页面小需求引入新的前端框架或状态管理库。
- 在路由层堆业务逻辑或外部连接细节。
- 在模板里写复杂数据整理逻辑。
- 手工维护 `requirements.txt` 版本号；应按 `doc/development.md` 使用 `uv export`。
- 修改 `config.json` 结构但不做迁移和测试。
- 给文件浏览器增加写操作能力。
- 让测试默认依赖正在运行的 Kafka、ZooKeeper 或 Elasticsearch。
